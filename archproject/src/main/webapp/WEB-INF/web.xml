<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns="http://java.sun.com/xml/ns/javaee"
	xsi:schemaLocation="http://java.sun.com/xml/ns/javaee 
						http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd"
	id="WebApp_ID" version="3.0">

	<display-name>Java EE Architecture Project</display-name>
	<welcome-file-list>
		<welcome-file>index.html</welcome-file>
	</welcome-file-list>

	<!-- JSF (Java Server Faces) servlet -->
	<servlet>
		<servlet-name>Faces Servlet</servlet-name>
		<servlet-class>javax.faces.webapp.FacesServlet</servlet-class>
		<load-on-startup>1</load-on-startup>
	</servlet>
	<servlet-mapping>
		<servlet-name>Faces Servlet</servlet-name>
		<url-pattern>*.xhtml</url-pattern>
	</servlet-mapping>

	<!-- Resources that need authentication -->
	<security-constraint>
		<web-resource-collection>
			<web-resource-name>Protected Pages</web-resource-name>
			<url-pattern>/pages/protected/*</url-pattern>
		</web-resource-collection>
		<auth-constraint>
			<role-name>LOGGED</role-name>
		</auth-constraint>
	</security-constraint>

	<!-- Allowed role for authenticating -->
	<security-role>
		<role-name>LOGGED</role-name>
	</security-role>

	<!-- The login prompt method as a basic javascript dialog -->
	<login-config>
		<auth-method>BASIC</auth-method>
		<realm-name>archproject</realm-name>
	</login-config>

	<!-- Change the default theme of primefaces to pepper-grinder -->
	<context-param>
		<param-name>primefaces.THEME</param-name>
		<param-value>pepper-grinder</param-value>
	</context-param>
</web-app>