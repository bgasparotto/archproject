/**
 * Package containing interfaces that can be used as an identifier for an
 * object, for example, a {@code Long} field named {@code id} that uniquely
 * distinguish one persisted entity from another of the same type.
 */
package com.bgasparotto.archproject.model.identity;