/**
 * Main package of {@code infrastructure} layer, which contains helper and
 * utility classes which are application wide. Classes from this layer shouldn't
 * access other layers, but should be accessed from any other layer.
 */
package com.bgasparotto.archproject.infrastructure;